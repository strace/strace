#!/bin/sh

# Check fstat syscall decoding.

. "${srcdir=.}/init.sh"

syscall=$NAME
run_prog > /dev/null
sample=stat.sample
> "$sample"
run_strace -ve$syscall -P$sample -a21 $args > "$EXP"
match_diff "$LOG" "$EXP"
