#!/bin/sh

# Check openat syscall decoding.

. "${srcdir=.}/init.sh"

run_prog > /dev/null
syscall=$NAME
sample=$syscall.sample
run_strace -e$syscall -P$sample $args > "$OUT"
match_diff "$LOG" "$OUT"
rm -f "$OUT"
